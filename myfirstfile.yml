---
- hosts : mywebserver
  become : yes
  tasks : 
    - name : i want to install webserver
      apt:
       name : apache2
       state : present
    - name : install webserver on redhat
      yum : 
       name : httpd
       state : present